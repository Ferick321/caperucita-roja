<?xml version="1.0" encoding="utf-8"?>
<!--
  Solo se admite trafico cifrado.

  Para desarrollo contra un servidor local sin certificado, descomenta el
  bloque domain-config con tu direccion IP: nunca lo dejes activo en la
  version que publiques.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>
    -->
</network-security-config>
